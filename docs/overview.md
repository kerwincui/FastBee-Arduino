# FastBee-Arduino 项目概述

FastBee-Arduino 是一个面向 ESP32 系列芯片的开源物联网固件框架,通过零代码可视化配置,让 ESP32 像搭积木一样秒变全能物联网设备。

## Web 控制台一览

烧录完成后，设备会提供本地 Web 控制台。仪表盘用于确认 IP、WiFi、资源和运行时间；移动端同样可以完成状态查看和基础维护。

![设备监控仪表盘](images/fastbee-dashboard.png)

![移动端仪表盘](images/fastbee-mobile-dashboard.png)

## 项目背景

随着物联网技术的快速发展,边缘计算设备需求激增。然而,传统嵌入式开发面临两大挑战:
- **开发门槛高**: 需要掌握 C/C++ 编程、硬件驱动、网络协议等专业知识
- **部署效率低**: 每次功能调整都需要重新编译烧录,现场维护成本高

ESP32 芯片虽然功能强大,但资源受限(520KB SRAM、4MB Flash),传统物联网框架往往占用过多资源,导致系统不稳定。

FastBee-Arduino 应运而生,采用**零代码、可视化配置**理念,通过内置 Web 管理界面完成所有配置,真正实现"烧录即用"。

## 项目目标

- **零代码开发**: 无需编写一行代码,通过浏览器完成外设配置、协议对接、规则编排
- **资源优化**: 针对 ESP32 资源特点,采用精简版/标准版/全功能版分层架构,优先保证系统稳定
- **快速部署**: 从烧录到上云仅需 5 步,适合从样机验证到批量部署
- **开放生态**: AGPL-3.0 开源协议,支持自定义外设驱动和协议扩展

## 核心特性

### 浏览器即控制台
- 设备监控、外设配置、协议对接、规则编排全部在本地 Web 界面完成
- 响应式设计,支持 PC/平板/手机访问
- 首次启动自动进入 AP 配网模式,无需额外工具

### 多联网方式
- **WiFi** (所有版本): AP/STA 双模,mDNS 域名访问
- **以太网** (标准版/全功能版): W5500 SPI 接口
- **4G蜂窝** (标准版/全功能版): EC801E-CN 模块
- **LoRa网关** (全功能版): E22-400T22D 透传

### 协议与外设一体化
- **通信协议**: MQTT；标准版/全功能版支持 Modbus RTU 主站、寄存器映射和子设备控制
- **外设支持**: GPIO、传感器、显示屏、数码管、RS485/UART 等 25+ 类型
- **规则引擎**: 4种触发器、27种动作,支持定时/事件/平台/轮询触发

### 面向现场稳定性
- AP/STA 入网自动切换,断线重连
- 内存门控(MEMGUARD)机制,低内存降级保护
- 按需加载、轻量化首屏,降低浏览器并发压力
- Worker池预创建,避免运行时动态分配

### 清晰的资源分层
- **精简版** (Lite): ESP32-C3/C6/S2 低成本节点,保留 WiFi/MQTT、mDNS、基础 Web、OLED/TM1637 和配置导入导出
- **标准版** (Standard): ESP32/ESP32-S3 主力固件,增加以太网、4G、Modbus、I2C/RFID/IR 等现场能力
- **全功能版** (Full): ESP32-S3 专用,保留 OTA、文件/日志、多用户、RuleScript、多语言、LoRa 和更多协议能力

## 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| **硬件** | ESP32/ESP32-C3/ESP32-S3 | 双核 240MHz, WiFi/BT/BLE |
| **框架** | Arduino + PlatformIO | 快速开发,多环境构建 |
| **通信** | WiFi/BLE/MQTT/Modbus RTU | 多协议支持 |
| **Web服务** | ESPAsyncWebServer | 异步 HTTP 服务器 |
| **前端** | 原生 JavaScript | 无框架依赖,轻量化 |
| **文件系统** | LittleFS | 嵌入式 Flash 文件系统 |
| **构建工具** | Node.js + PlatformIO | Web资源压缩、固件编译 |

## 版本对比

| 特性 | 精简版 Lite | 标准版 Standard | 全功能版 Full |
|------|--------|--------|--------|
| **适用芯片** | ESP32-C3/C6/S2 | ESP32 / ESP32-S3 | ESP32-S3 |
| **Flash需求** | 4MB | 4MB+ | 8MB+ |
| **Web界面** | 核心页面 | 标准页面 | 完整页面 |
| **联网方式** | WiFi | WiFi + 以太网 + 4G | WiFi + 以太网 + 4G + LoRa |
| **MQTT** | ✓ | ✓ | ✓ |
| **Modbus RTU 主站** | ✗ | ✓ | ✓ |
| **多用户/角色/OTA/文件日志** | ✗ | ✗ | ✓ |
| **多语言** | ✗ | ✗ | ✓ |
| **高级传感器** | DHT/DS18B20/超声波/I2C轻量 | + BMP280/MPU6050/RFID/红外 | 全部 |

**选型建议**:
- 低成本 WiFi/MQTT 节点优先使用 `esp32c3` 或 `esp32c6`
- 需要以太网、4G、Modbus、BMP280、MPU6050、RFID、红外等模块时使用 `esp32` 或 `esp32s3`
- 需要 OTA、多用户、文件/日志、RuleScript、多语言或 LoRa 时使用 `esp32s3-full`
- 长期运行场景优先保证稳定性,避免开启高资源功能

## 应用场景

### 智能家居原型开发
- 温湿度监控自动开空调
- 光照传感器控制照明
- 门窗磁感应报警

### 工业传感器数据采集
- Modbus RTU 采集工业传感器
- 电流/电压监测过载保护
- 多设备轮询数据上报

### 农业物联网监测
- 土壤温湿度监测
- 光照/CO2浓度采集
- 自动灌溉控制

### 教育实验平台
- 48个示例涵盖所有常见传感器
- 零代码配置,专注原理学习
- 快速验证硬件方案

### 轻量现场终端部署
- 烧录即用,无需现场编程
- Web 远程配置和维护
- 配置导入导出批量部署

## 相关文档

- [快速开始](quick-start.md) - 5步完成首次配置
- [架构设计](architecture.md) - 系统架构和模块关系
- [核心框架](core-framework.md) - 主要组件和关键类
- [开发指南](development-guide.md) - 环境搭建和贡献规范
- [用户手册](user-manual.md) - 完整操作手册

---

**文档版本**: v1.0  
**最后更新**: 2026-06-04  
**维护者**: FastBee团队
